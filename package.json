{
  "name": "overlay-monster",
  "version": "0.2.7",
  "private": true,
  "description": "Protected AI overlay browser desktop application.",
  "author": "Aditya Shinde",
  "main": "dist/electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:web && npm run build:electron",
    "build:electron": "vite build --mode electron && tsc -p tsconfig.electron.json",
    "build:web": "vite build --mode web",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "electron": "electron .",
    "dist:win": "npm run build && electron-builder --win nsis --x64"
  },
  "dependencies": {
    "electron-updater": "^4.0.0",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "electron": "latest",
    "electron-builder": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "build": {
    "appId": "com.overlaymonster.browser",
    "productName": "Overlay Monster",
    "artifactName": "Overlay-Monster-Setup.exe",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "adityaztpl",
        "repo": "Overlay-Monster",
        "releaseType": "release"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
